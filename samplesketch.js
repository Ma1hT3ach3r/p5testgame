function setup() {
  createCanvas(400,400);

//background(50,50,50);
}

function draw() {
  background(220);
  fill(mouseX,25,56);
  for(int i=0, i++,i<mouseX||i<mouseY){
  ellipse(mouseX,mouseY,10,10);
  }
}
